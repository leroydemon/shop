namespace DbLevel.SortableFields
{
    public enum StorageSortableFields
    {
        Id,
        CreateDateTime,
        UpdateDateTime,
        Name,
        City,
        Address,
        Phone
    }
}
